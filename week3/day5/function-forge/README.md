# Function Forge Project

### Functions
- `calculateTotalWithTax(total, taxRate)`
- `strToTitle(str)`
- `isPrime(num)`
- `isPalindrome(str)`

### How To Run
1. Install node
2. Clone this repo
3. Open a terminal and run `node main.js`